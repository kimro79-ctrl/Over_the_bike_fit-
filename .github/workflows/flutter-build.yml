name: BikeFit Final Ultra Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 1️⃣ 환경 재생성 (v2 Embedding 적용)
        run: |
          rm -rf android
          flutter create . --platforms android --org com.overthebike --project-name over_the_bike_fit
          flutter pub get

      - name: 2️⃣ 키스토어 복원 및 검증
        run: |
          mkdir -p android/app
          # Secrets 데이터를 파일로 변환
          echo "${{ secrets.RELEASE_KEYSTORE }}" | base64 --decode > android/app/release.jks
          
          # 파일 크기 확인 (0바이트면 에러 발생)
          ls -la android/app/release.jks
          if [ ! -s android/app/release.jks ]; then
            echo "ERROR: release.jks is 0 bytes. Check your GitHub Secret 'RELEASE_KEYSTORE'!"
            exit 1
          fi

      - name: 3️⃣ 서명 로직 및 권한 강제 주입
        run: |
          # build.gradle에 서명(signingConfigs) 로직 주입
          cat <<EOF > android/app/build.gradle
          def keystoreProperties = new Properties()
          def keystorePropertiesFile = rootProject.file('key.properties')
          if (keystorePropertiesFile.exists()) {
              keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
          }
          plugins {
              id "com.android.application"
              id "kotlin-android"
              id "dev.flutter.flutter-gradle-plugin"
          }
          android {
              namespace "com.overthebike.over_the_bike_fit"
              compileSdk 34
              signingConfigs {
                  release {
                      keyAlias keystoreProperties['keyAlias']
                      keyPassword keystoreProperties['keyPassword']
                      storeFile keystoreProperties['storeFile'] ? file(keystoreProperties['storeFile']) : null
                      storePassword keystoreProperties['storePassword']
                  }
              }
              buildTypes {
                  release {
                      signingConfig signingConfigs.release
                      minifyEnabled true
                      proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
                  }
              }
              defaultConfig {
                  applicationId "com.overthebike.over_the_bike_fit"
                  minSdk 21
                  targetSdk 34
                  versionCode 15  # 기존보다 높은 버전 코드
                  versionName "1.5.0"
              }
          }
          flutter { source '../..' }
          EOF

      - name: 4️⃣ 서명 정보 최종 설정
        run: |
          # 별칭(Alias)은 파일 데이터에 기록된 'fresh-upload-key'를 사용합니다.
          cat <<EOF > android/key.properties
          storePassword=${{ secrets.KEYSTORE_PASSWORD }}
          keyPassword=${{ secrets.KEY_PASSWORD }}
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

      - name: 5️⃣ Build Release AAB
        run: |
          flutter clean
          flutter pub get
          flutter build appbundle --release --no-tree-shake-icons

      - name: 6️⃣ 최종 서명 지문 확인
        run: |
          keytool -printcert -jarfile build/app/outputs/bundle/release/app-release.aab | grep SHA1

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_Final_Success
          path: build/app/outputs/bundle/release/app-release.aab

name: BikeFit V2-Fix & Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🛠️ V1 임베딩 강제 제거 및 최신화 설정
        run: |
          flutter clean

          # 1. 패키지명과 경로를 indoor_bike_fit으로 일치 (중요)
          PACKAGE_NAME="com.kimro79.indoor_bike_fit"
          PACKAGE_PATH="android/app/src/main/kotlin/com/kimro79/indoor_bike_fit"
          
          # 기존의 잘못된 MainActivity 삭제 후 V2 규격으로 생성
          find android/app/src/main -name "MainActivity.*" -delete
          mkdir -p $PACKAGE_PATH
          cat <<EOF > $PACKAGE_PATH/MainActivity.kt
          package $PACKAGE_NAME
          import io.flutter.embedding.android.FlutterActivity
          class MainActivity: FlutterActivity()
          EOF

          # 2. AndroidManifest.xml에서 V1 에러를 유발하는 구식 설정들 강제 삭제
          MANIFEST="android/app/src/main/AndroidManifest.xml"
          sed -i '/android:name="io.flutter.app.FlutterApplication"/d' $MANIFEST
          sed -i '/io.flutter.embedding.android.NormalThemes/d' $MANIFEST
          sed -i '/io.flutter.embedding.android.SplashScreenDrawable/d' $MANIFEST

          # 3. key.properties 생성 (사용자님이 넣은 android/app/release.jks 참조)
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

          # 4. build.gradle 서명 설정 강제 적용
          if [ -f "android/app/build.gradle" ]; then
            sed -i 's/signingConfig signingConfigs.debug/signingConfig signingConfigs.release/g' android/app/build.gradle
          fi

          flutter pub get

      - name: AAB 빌드 실행 (V2 방식으로 빌드)
        run: flutter build appbundle --release --no-tree-shake-icons

      - name: ★최종 결과물 다운로드★
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_V2_Fixed_AAB
          path: build/app/outputs/bundle/release/app-release.aab

name: BikeFit V1 Ghost Buster Build
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🔐 [v1 잔재 삭제 및 키 고정]
        run: |
          flutter clean
          
          # 1. v1 Java 잔재 파일 자동 삭제 (사용자님 제안 로직)
          find android -name "*GeneratedPluginRegistrant.java" -delete || true
          find android -name "*FlutterMain*" -delete || true
          
          # 2. Manifest 및 build.gradle v1 참조 제거 (사용자님 제안 로직)
          sed -i 's/android:name="io.flutter.app.FlutterApplication"/android:name="${applicationName}"/g' android/app/src/main/AndroidManifest.xml || true
          sed -i '/io.flutter.app.FlutterApplication/d' android/app/build.gradle || true

          # 3. MainActivity를 v2 표준으로 강제 교체 (범인 체포)
          find android/app/src/main -name "MainActivity.*" -delete
          mkdir -p android/app/src/main/kotlin/com/overthebike/over_the_bike_fit
          cat <<EOF > android/app/src/main/kotlin/com/overthebike/over_the_bike_fit/MainActivity.kt
          package com.overthebike.over_the_bike_fit
          import io.flutter.embedding.android.FlutterActivity
          class MainActivity: FlutterActivity()
          EOF

          # 4. key.properties 설정 (사용자님의 release.jks 연결)
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF
          
          # 5. 서명 설정 강제 적용
          if [ -f "android/app/build.gradle" ]; then
            sed -i 's/signingConfig signingConfigs.debug/signingConfig signingConfigs.release/g' android/app/build.gradle
          fi

          flutter pub get
          flutter pub upgrade --major-versions

      - name: AAB 빌드 실행
        run: flutter build appbundle --release --no-tree-shake-icons

      - name: ★성공한 AAB 파일 다운로드★
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_V2_CLEAN_AAB
          path: build/app/outputs/bundle/release/app-release.aab

name: BikeFit FINAL Absolute Clean Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: ☕ Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🧹 환경 초기화 및 플러그인 최신화
        run: |
          echo "🧼 Cleaning build and cache..."
          flutter clean
          rm -rf ~/.pub-cache
          rm -rf $FLUTTER_ROOT/.pub-cache
          flutter pub cache repair || true
          echo "📦 Upgrading dependencies..."
          flutter pub upgrade --major-versions
          flutter pub outdated || true
          flutter pub get

          echo "✅ Ensuring correct Android structure..."
          mkdir -p android/app/src/main/kotlin/com/overthebike/over_the_bike_fit
          cat <<EOF > android/app/src/main/kotlin/com/overthebike/over_the_bike_fit/MainActivity.kt
          package com.overthebike.over_the_bike_fit
          import io.flutter.embedding.android.FlutterActivity
          class MainActivity: FlutterActivity()
          EOF

          cat <<EOF > android/app/src/main/AndroidManifest.xml
          <manifest xmlns:android="http://schemas.android.com/apk/res/android"
              package="com.overthebike.over_the_bike_fit">
              <application
                  android:label="over_the_bike_fit"
                  android:name="\${applicationName}"
                  android:icon="@mipmap/ic_launcher">
                  <activity
                      android:name=".MainActivity"
                      android:exported="true"
                      android:launchMode="singleTop"
                      android:theme="@style/LaunchTheme"
                      android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|screenLayout|density|fontScale"
                      android:hardwareAccelerated="true"
                      android:windowSoftInputMode="adjustResize">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN"/>
                          <category android:name="android.intent.category.LAUNCHER"/>
                      </intent-filter>
                  </activity>
                  <meta-data
                      android:name="flutterEmbedding"
                      android:value="2" />
              </application>
          </manifest>
          EOF

          echo "🧹 Removing legacy GeneratedPluginRegistrant (V1 embedding)"
          rm -rf android/app/src/main/java/io/flutter/plugins/GeneratedPluginRegistrant.java
          echo "✅ Android structure verified."

      - name: 🔐 Keystore 설정
        run: |
          echo "🔑 Setting up keystore..."
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

          if [ ! -f "android/app/release.jks" ]; then
            echo "📦 Copying release.jks..."
            cp $GITHUB_WORKSPACE/release.jks android/app/release.jks
          else
            echo "✅ release.jks already exists. Skipping copy."
          fi

      - name: 🔍 SHA1 인증서 지문 출력
        run: |
          echo "🔎 Extracting SHA1 from release.jks..."
          keytool -list -v \
            -keystore android/app/release.jks \
            -alias fresh-upload-key \
            -storepass Sodomaki80 \
            -keypass Sodomaki80 | grep "SHA1"

      - name: 🧩 V1 임베딩 코드 탐지
        run: |
          echo "🔍 Searching for legacy Android V1 plugin code..."
          grep -r "registerWith" . || echo "✅ No V1 embedding code found"

      - name: 🧰 플러그인 자동 패치 (V1 코드 제거)
        run: |
          echo "🧩 Searching for legacy registerWith in plugin cache..."
          grep -rl "registerWith" $FLUTTER_ROOT/.pub-cache || true
          grep -rl "registerWith" $HOME/.pub-cache || true
          find $HOME/.pub-cache -type f -name "*.java" -print0 | xargs -0 sed -i 's/registerWith/ignoredRegisterWith/g' || true
          echo "✅ Patched any legacy registerWith() calls inside plugins."

      - name: 🚀 AAB 빌드 실행
        run: |
          echo "🚀 Building AAB..."
          flutter build appbundle --release --no-tree-shake-icons

      - name: 📦 최종 AAB 파일 다운로드
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_FINAL_SIGNED_AAB
          path: build/app/outputs/bundle/release/*.aab

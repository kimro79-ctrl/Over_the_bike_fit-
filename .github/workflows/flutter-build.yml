name: BikeFit Manifest Fix Build
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🔐 [AndroidManifest 수리 및 키 설정]
        run: |
          # 1. 빌드 청소
          flutter clean
          flutter pub get

          # 2. AndroidManifest.xml 정밀 수리 (v1 embedding 해결)
          # application 태그의 android:name 부분을 최신 규격으로 교체하거나 삭제합니다.
          sed -i 's/android:name="io.flutter.app.FlutterApplication"//g' android/app/src/main/AndroidManifest.xml
          
          # 3. key.properties 생성 (사용자님이 옮긴 android/app/ 위치 고정)
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

          # 4. debug 대신 release 서명 강제 적용
          if [ -f "android/app/build.gradle" ]; then
            sed -i 's/signingConfig signingConfigs.debug/signingConfig signingConfigs.release/g' android/app/build.gradle
          fi

      - name: AAB 빌드 실행
        run: flutter build appbundle --release --no-tree-shake-icons

      - name: ★최종 결과물 다운로드★
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_MANIFEST_FIXED_AAB
          path: build/app/outputs/bundle/release/app-release.aab

name: BikeFit - FINAL FORCED BUILD SUCCESS

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v4

      - name: ☕ Java 17 및 Flutter 설정
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🐦 Flutter 설정 (3.27.1)
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🏗️ [1단계] 사용자님 제안: V1 정밀 타격 및 경로 청소
        run: |
          flutter clean
          # 1. 모든 위치에서 GeneratedPluginRegistrant 관련 파일 제거
          find . -name "*GeneratedPluginRegistrant*" -exec rm -rf {} + || true
          
          # 2. 정석적인 Java/Kotlin 경로 확보
          mkdir -p android/app/src/main/java/io/flutter/plugins
          mkdir -p android/app/src/main/kotlin/com/overthebike/over_the_bike_fit

          # 💡 [핵심 보완] 파일 생성을 원천 차단하기 위해 '동일 이름의 폴더' 생성
          # 이렇게 하면 엔진이 파일을 쓰고 싶어도 "같은 이름의 폴더가 있어 실패"하게 됩니다.
          mkdir -p android/app/src/main/java/io/flutter/plugins/GeneratedPluginRegistrant.java

      - name: 🔑 [2단계] 키스토어 및 서명 설정 (루트 경로 활용)
        run: |
          # 루트에 있는 release.jks를 복사
          cp release.jks android/app/release.jks
          
          cat <<EOF > android/app/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

      - name: 🚀 [3단계] 필살기: AAB 빌드 실행 (V1 우회)
        run: |
          # pub get 단계에서 생기는 쓰레기들 차단
          flutter pub get
          
          # 빌드 직전, 다시 한번 가짜 폴더 '알박기' 확인
          rm -f android/app/src/main/java/io/flutter/plugins/GeneratedPluginRegistrant.java
          mkdir -p android/app/src/main/java/io/flutter/plugins/GeneratedPluginRegistrant.java
          
          # --no-pub 옵션을 써서 빌드 중 파일 생성을 막습니다
          flutter build appbundle --release --no-tree-shake-icons --no-pub

      - name: 📦 [성공] AAB 결과물 추출
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_Final_AAB
          path: |
            build/app/outputs/bundle/release/*.aab
            build/**/*.aab
          if-no-files-found: error

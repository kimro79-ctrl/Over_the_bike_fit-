name: BikeFit Google Console Upload
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🔐 fresh-upload-key 별칭으로 서명 고정
        run: |
          # 1. 낡은 V1 에러 방지를 위해 안드로이드 설정 갱신
          flutter create . --platforms android --overwrite

          # 2. 저장소 루트의 release.jks를 앱 폴더로 복사
          mkdir -p android/app
          cp release.jks android/app/release.jks

          # 3. [핵심] 별칭 오타 수정: fresh-upload-key (하이픈 확인)
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

          # 4. 빌드 서버가 임의의 키를 생성하지 못하도록 release 서명 강제 적용
          if [ -f "android/app/build.gradle" ]; then
            sed -i 's/signingConfig signingConfigs.debug/signingConfig signingConfigs.release/g' android/app/build.gradle
          fi

          flutter pub get

      - name: AAB 빌드 실행
        run: flutter build appbundle --release --no-tree-shake-icons

      - name: ★이 파일을 다운로드하여 구글에 올리세요★
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_FINAL_FOR_CONSOLE
          path: build/app/outputs/bundle/release/app-release.aab

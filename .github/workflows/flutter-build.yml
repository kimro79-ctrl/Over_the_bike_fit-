name: BikeFit OverTheBike - GET MY AAB

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: ☕ Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🛠️ 필수 환경 설정 (성공 당시 환경 복구)
        run: |
          # 1. 패키지 경로 정리
          PACKAGE_NAME="com.overthebike.over_the_bike_fit"
          PACKAGE_PATH="android/app/src/main/kotlin/com/overthebike/over_the_bike_fit"
          mkdir -p $PACKAGE_PATH
          
          # 2. MainActivity.kt 생성
          cat <<EOF > $PACKAGE_PATH/MainActivity.kt
          package $PACKAGE_NAME
          import io.flutter.embedding.android.FlutterActivity
          class MainActivity: FlutterActivity()
          EOF

          # 3. 키스토어 및 빌드 정보 설정
          cat <<EOF > android/app/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF
          
          flutter pub get

      - name: 🚀 AAB 빌드 실행
        run: |
          # V1 에러가 날 경우에만 대비하여 해당 파일만 살짝 지워줍니다.
          rm -rf android/app/src/main/java/io/flutter/plugins/GeneratedPluginRegistrant.java
          flutter build appbundle --release --no-tree-shake-icons

      - name: 📦 [핵심] 모든 경로에서 AAB 찾아내기
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_Final_AAB_File
          # 특정 경로가 아닌, 빌드 폴더 전체에서 .aab 파일을 찾아냅니다.
          path: build/**/*.aab
          if-no-files-found: error

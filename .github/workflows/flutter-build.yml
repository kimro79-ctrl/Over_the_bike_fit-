name: BikeFit Real Final Success
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🔐 [핵심] 진짜 별칭 fresh-upload-key 강제 연결
        run: |
          # 1. 낡은 안드로이드 설정 갱신 (V1 에러 방지)
          flutter create . --platforms android --overwrite

          # 2. 저장소 루트의 release.jks를 앱 폴더로 복사
          mkdir -p android/app
          cp release.jks android/app/release.jks

          # 3. 별칭 고정: fresh-upload-key
          # 이 이름이 틀리면 빌드 서버가 가짜 키를 생성합니다.
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

          # 4. 빌드 서버가 임의의 키를 생성하지 못하도록 릴리스 서명 강제 고정
          if [ -f "android/app/build.gradle" ]; then
            sed -i 's/signingConfig signingConfigs.debug/signingConfig signingConfigs.release/g' android/app/build.gradle
          fi

          flutter pub get

      - name: AAB 빌드 실행
        run: flutter build appbundle --release --no-tree-shake-icons

      - name: ★최종 결과물 다운로드★
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_FINAL_SUCCESS
          path: build/app/outputs/bundle/release/app-release.aab

name: BikeFit Final Success Build
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 안드로이드 설정 및 JKS 서명 연결 (V2 embedding 변환 포함)
        run: |
          echo "📱 Android V2 embedding 구조 최신화 중..."
          # 기존 안드로이드 프로젝트를 덮어쓰지 않고 v2 embedding으로 강제 업데이트
          flutter create . --platforms android --overwrite

          echo "🧩 Manifest 패키지 중복 제거"
          find android/app/src/main -name "AndroidManifest.xml" -exec sed -i 's/package="com.overthebike.over_the_bike_fit"//g' {} +

          echo "🔑 release.jks 복사"
          mkdir -p android/app
          cp release.jks android/app/release.jks || echo "⚠️ 루트에 release.jks가 없습니다!"

          echo "🔐 key.properties 생성"
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

          echo "🛠 build.gradle 수정"
          if [ -f "android/app/build.gradle" ]; then
            sed -i 's/signingConfig signingConfigs.debug/signingConfig signingConfigs.release/g' android/app/build.gradle
          fi

          echo "🚫 V1 임베딩 코드 자동 교체"
          # MainActivity.kt 경로 자동 탐색 후 import 수정
          MAIN_ACTIVITY=$(find android/app/src/main -name "MainActivity.kt" | head -n 1)
          if [ -f "$MAIN_ACTIVITY" ]; then
            sed -i 's/import io.flutter.app.FlutterActivity/import io.flutter.embedding.android.FlutterActivity/g' "$MAIN_ACTIVITY"
            sed -i '/GeneratedPluginRegistrant/d' "$MAIN_ACTIVITY"
          fi

          # Gradle 호환성 옵션
          echo "android.useAndroidX=true" >> android/gradle.properties
          echo "android.enableJetifier=true" >> android/gradle.properties

          flutter clean
          flutter pub get

      - name: AAB 빌드 실행
        run: flutter build appbundle --release --no-tree-shake-icons

      - name: ★최종 결과물 다운로드★
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_SUCCESS_AAB
          path: build/app/outputs/bundle/release/app-release.aab

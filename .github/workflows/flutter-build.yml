name: BikeFit - Absolute Final Fix 2026
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'

      - name: 🔥 Fix Android V1 Embedding & Inject Verified Key
        run: |
          # 1. 빌드 환경 초기화
          flutter clean
          rm -rf android/app/build android/key.properties
          
          # 2. V1 Embedding 에러 해결을 위해 안드로이드 프로젝트 강제 갱신
          # (이미지 1000013293.jpg의 에러를 해결하는 핵심 단계입니다)
          flutter create . --platforms android --overwrite
          
          # 3. 검증된 release.jks 파일을 앱 폴더로 강제 주입
          mkdir -p android/app
          cp release.jks android/app/release.jks || exit 1

          # 4. 서명 설정(key.properties) 강제 생성
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

      - name: Build App Bundle (Release)
        run: |
          flutter pub get
          # 아이콘 쉐이킹 문제 방지 및 릴리즈 빌드
          flutter build appbundle --release --no-tree-shake-icons

      - name: 🚨 FINAL CHECK: FINGERPRINT 🚨
        run: |
          echo "===================================================="
          echo "빌드된 파일의 SHA1을 확인합니다 (반드시 75:CD... 여야 함)"
          keytool -printcert -jarfile build/app/outputs/bundle/release/app-release.aab | grep SHA1
          echo "===================================================="

      - name: Upload AAB
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_Final_Ready_To_Upload
          path: build/app/outputs/bundle/release/app-release.aab

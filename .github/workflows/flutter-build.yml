name: BikeFit Final Success Fixed
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java & Flutter
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🔐 진짜 키(75:CD) 강제 고정 및 설정 초기화
        run: |
          # 1. 꼬인 안드로이드 설정 완전 초기화
          flutter create . --platforms android --overwrite

          # 2. [중요] 2개의 jks 중 진짜를 선택해서 android/app으로 복사
          # 사용자님이 리포지토리 루트에 두신 그 '진짜' release.jks를 사용합니다.
          mkdir -p android/app
          cp release.jks android/app/release.jks

          # 3. key.properties 설정 (20일 재등록 당시 정보로 고정)
          cat <<EOF > android/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF

          # 4. 가짜(디버그) 키 생성을 법적으로 금지
          if [ -f "android/app/build.gradle" ]; then
            sed -i 's/signingConfig signingConfigs.debug/signingConfig signingConfigs.release/g' android/app/build.gradle
          fi

          flutter pub get

      - name: AAB 빌드 실행
        run: flutter build appbundle --release --no-tree-shake-icons

      - name: ★최종 결과물 다운로드★
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_REAL_SUCCESS_AAB
          path: build/app/outputs/bundle/release/app-release.aab

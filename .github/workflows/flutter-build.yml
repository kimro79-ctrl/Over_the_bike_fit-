name: BikeFit UI Fix - Victory Run (Auto Versioning)

on:
  workflow_dispatch: # 수동으로 빌드 실행

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v4 

      - name: ☕ Java 및 Flutter 설정 (JDK 17)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.27.1 

      - name: 🔢 빌드 번호 계산 (Base 1707 + Run Number)
        id: version_logic
        run: |
          # 기본 1707부터 시작해서 빌드 횟수(run_number)만큼 더합니다.
          # 첫 실행 시 1707 + 1 = 1708이 되도록 세팅 (원하시면 1706 + github.run_number로 조절 가능)
          BASE_CODE=1706
          NEW_CODE=$((BASE_CODE + ${{ github.run_number }}))
          echo "FINAL_VERSION_CODE=$NEW_CODE" >> $GITHUB_ENV
          echo "Generated Version Code: $NEW_CODE"

      - name: 🧹 [초강력 클린] 캐시 삭제 및 v2 임베딩 강제 초기화
        run: |
          # 1. 이전 빌드 찌꺼기 완전 삭제
          flutter clean
          rm -rf android
          
          # 2. 최신 Flutter 버전 기반으로 Android 프로젝트 재생성 (v2 임베딩 자동 적용)
          flutter create . --platforms android --org com.overthebike --project-name over_the_bike_fit
          flutter pub get 

      - name: 🔑 Keystore 복원
        env:
          KEY_DATA: ${{ secrets.RELEASE_KEYSTORE }}
        run: |
          mkdir -p android/app
          echo "$KEY_DATA" | tr -d '\n ' | base64 --decode > android/app/release.jks 

      - name: 🔧 key.properties 생성
        run: |
          cat <<EOF > android/app/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF 

      - name: 🔍 [검증] SHA-1 지문 확인
        run: |
          keytool -list -v -keystore android/app/release.jks -alias fresh-upload-key -storepass Sodomaki80 -keypass Sodomaki80 | grep SHA1 

      - name: 📑 build.gradle 직접 작성 (버전 자동 주입)
        run: |
          # 변수 오작동을 막기 위해 EOF_GRADLE 이라는 고유 태그를 사용합니다.
          cat <<EOF_GRADLE > android/app/build.gradle
          plugins {
              id "com.android.application"
              id "kotlin-android"
              id "dev.flutter.flutter-gradle-plugin"
          } 

          def keystoreProperties = new Properties()
          def keystorePropertiesFile = project.file('key.properties')
          if (keystorePropertiesFile.exists()) {
              keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
          } 

          android {
              namespace "com.overthebike.over_the_bike_fit"
              compileSdk 35 

              compileOptions {
                  sourceCompatibility JavaVersion.VERSION_17
                  targetCompatibility JavaVersion.VERSION_17
              } 

              kotlinOptions {
                  jvmTarget = '17'
              } 

              defaultConfig {
                  applicationId "com.overthebike.over_the_bike_fit"
                  minSdk 21
                  targetSdk 35
                  versionCode ${env.FINAL_VERSION_CODE}
                  versionName "1.0.1"
              } 

              signingConfigs {
                  release {
                      keyAlias keystoreProperties['keyAlias']
                      keyPassword keystoreProperties['keyPassword']
                      storeFile file(keystoreProperties['storeFile'])
                      storePassword keystoreProperties['storePassword']
                  }
              } 

              buildTypes {
                  release {
                      signingConfig signingConfigs.release
                      minifyEnabled false
                      shrinkResources false
                  }
              }
          } 

          flutter {
              source '../..'
          }
          EOF_GRADLE

      - name: 🏗️ AAB 빌드 실행 (Version: ${{ env.FINAL_VERSION_CODE }})
        run: flutter build appbundle --release --no-tree-shake-icons 

      - name: 📦 수정된 AAB 파일 업로드
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_v${{ env.FINAL_VERSION_CODE }}_Victory_AAB
          path: build/app/outputs/bundle/release/app-release.aab

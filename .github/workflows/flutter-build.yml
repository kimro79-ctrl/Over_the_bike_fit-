name: BikeFit - Java Structure Fix & Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v4

      - name: ☕ Java 17 및 Flutter 설정
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🐦 Flutter 설정 (3.27.1)
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: 🏗️ [구조 정상화] Java/Kotlin 공존 설정
        run: |
          flutter clean
          # 1. 기존에 잘못 생성된 'java 파일'이 있다면 제거
          rm -f android/app/src/main/java
          
          # 2. 정석적인 java 폴더 구조 생성 (엔진 충돌 방지)
          mkdir -p android/app/src/main/java/io/flutter/plugins
          
          # 3. 현재 트리에 맞는 Kotlin 경로 보장
          mkdir -p android/app/src/main/kotlin/com/overthebike/over_the_bike_fit
          # MainActivity는 이미 있으므로 경로만 확실히 합니다.

      - name: 🔑 키스토어 및 서명 설정
        run: |
          cat <<EOF > android/app/key.properties
          storePassword=Sodomaki80
          keyPassword=Sodomaki80
          keyAlias=fresh-upload-key
          storeFile=release.jks
          EOF
          cp release.jks android/app/release.jks

      - name: 🚀 AAB 빌드 (Java 좀비 파일만 정밀 타격)
        run: |
          flutter pub get
          # 💡 핵심: java 폴더는 두되, 에러 원인인 파일만 빌드 직전에 지웁니다.
          rm -f android/app/src/main/java/io/flutter/plugins/GeneratedPluginRegistrant.java
          
          # 빌드 중 재생성을 막기 위해 --no-pub 옵션 사용
          flutter build appbundle --release --no-tree-shake-icons --no-pub

      - name: 📦 AAB 파일 업로드
        uses: actions/upload-artifact@v4
        with:
          name: BikeFit_JavaFix_AAB
          path: build/app/outputs/bundle/release/*.aab

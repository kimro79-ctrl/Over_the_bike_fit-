name: over_the_bike_fit
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+4

environment:
  sdk: '>=3.6.0 <4.0.0'  # Flutter 3.27.1 이상 대응

dependencies:
  flutter:
    sdk: flutter

  # 최신 안정 플러그인들
  fl_chart: ^0.70.0
  intl: ^0.19.0
  shared_preferences: ^2.3.5
  flutter_blue_plus: ^1.35.0     # ✅ 최신 V2 embedding 대응 버전
  permission_handler: ^12.0.1    # ✅ Android 14 + V2 embedding 완전 지원
  table_calendar: ^3.1.2
  cupertino_icons: ^1.0.6

  # 일부 플러그인이 필요로 하는 V2 라이프사이클 보조 패키지
  flutter_plugin_android_lifecycle: ^2.0.20

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

name: over_the_bike_fit
description: Final Legacy Build
version: 1.0.0+1

environment:
  # 옛날 Flutter 버전과 호환되도록 낮춥니다.
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  # 최신 라이브러리들은 옛날 환경에서 에러를 낼 수 있으므로 버전을 고정하거나 뺍니다.
  flutter_blue_plus: ^1.10.0
  fl_chart: ^0.50.0
  permission_handler: ^10.0.0
  shared_preferences: ^2.0.0
